<?xml version="1.0" encoding="utf-8"?>

<ScrollView android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android">

<LinearLayout
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:padding="30dp"
    android:orientation="vertical"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/coilImage"
        android:background="@drawable/rounded_shape"
        android:textStyle="bold"
        android:layout_width="match_parent"
        android:text="Coil(Kotlin)"
        android:layout_height="wrap_content"/>

    <Button
        android:id="@+id/cropImage"
        android:layout_marginTop="20dp"
        android:background="@drawable/rounded_shape"
        android:textStyle="bold"
        android:layout_width="match_parent"
        android:text="Corp Image(Kotlin)"
        android:layout_height="wrap_content"/>

    <Button
        android:id="@+id/picassoImage"
        android:layout_width="match_parent"
        android:background="@drawable/rounded_shape"
        android:textStyle="bold"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="Picasso(Kotlin)"/>



    <Button
        android:id="@+id/glideImage"
        android:layout_width="match_parent"
        android:background="@drawable/rounded_shape"
        android:textStyle="bold"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="Glide(Kotlin)"/>

    <Button
        android:id="@+id/universalImage"
        android:layout_width="match_parent"
        android:background="@drawable/rounded_shape"
        android:textStyle="bold"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="Universal Image Loader(Kotlin)"/>


    <Button
        android:id="@+id/frescoImage"
        android:layout_width="match_parent"
        android:background="@drawable/rounded_shape"
        android:textStyle="bold"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="Fresco(Kotlin)"/>

</LinearLayout>

</ScrollView>