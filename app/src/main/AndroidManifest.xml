<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="net.kathir.imagelibraries">

    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CoilUIActivity"/>
        <activity android:name=".CropImageActivity"/>
        <activity android:name=".PicassoImageActivity"/>
        <activity android:name=".PicassoImageViewActivity"/>
        <activity android:name=".GlideImageActivity"/>
        <activity android:name=".GlideImageViewActivity"/>
        <activity android:name=".UniversalImageActivity"/>
        <activity android:name=".UniversalImageViewActivity"/>
        <activity android:name=".FrescoImageActivity"/>
        <activity android:name=".FrescoImageViewActivity"/>
    </application>

</manifest>